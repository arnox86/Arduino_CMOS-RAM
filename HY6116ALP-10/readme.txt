
readFromRAM (/adress/)   // Gives out data standing at adress (byte, char, int, etc.)
writeToRAM (/adress/, /data (char)/)   // Writes data to adressed position

Adress can have up to 11 bits (0x0000 to 0x7FF).
